spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agms_telemetry_db?createDatabaseIfNotExist=true
    username: root
    password: Ijse@1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

# Standard logging for debugging sensor data
logging:
  level:
    lk.ijse.sensor_telemetry_service: DEBUG
